; MT5 startup config used by xau-launch.bat (and the watchdog's MT5 restart):
; force algo trading ON and auto-attach the EA at every launch.
;
; [StartUp] opens a fresh XAUUSD M5 chart each boot and attaches XauAssistant
; with its SOURCE-DEFAULT inputs (no ExpertParameters .set on purpose: the
; defaults are the owner-approved canon, and the service overrides mode &
; rules on the first heartbeat anyway). A profile-restored copy of the EA may
; coexist for a few seconds — the EA's single-instance guard (newest
; attachment wins) resolves that; see XauAssistant.mq5.
[Experts]
Enabled=1
[StartUp]
Expert=XauAssistant
Symbol=XAUUSD
Period=M5
